# Specs

What's being built, before it's built — see `.template.md` for the shape.

| Spec | Status | Date |
|---|---|---|

New specs go at the **bottom** of the table, in the order they're written —
never re-sorted or reformatted. An append lands in its own diff region and
never conflicts with anyone else's; a re-sort touches every row and
conflicts with everyone's.
